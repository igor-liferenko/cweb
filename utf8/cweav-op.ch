TODO: change 'sizeof @[@](' to 'sizeof (' everywhere,
and in this change-file add the space automatically;
and the same concerns cast if it precedes parentheses - see ~/pcm/generate-tone.w for an example
(because sizeof and (type) are operators, so there should be a space after them)
