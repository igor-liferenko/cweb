Treat "not_eq" as an identifier (not as an operator), because I do not use C++
(and the "@s not_eq normal" line does not make any difference if this change-file is applied):
@x
id_lookup("not_eq",NULL,alfop);
@y
@z
