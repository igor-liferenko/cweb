% when "sigaction" is used as a type, to make it bold use @= trick;
% as a bonus, it becomes a reserved word (i.e., not shows up in index)
\def\beginbold{\egroup\bgroup\let\vb=\&\bgroup}
\def\endbold{\egroup\egroup\bgroup}

% use @= trick for inner variables of structures from header files not to put them in index
% (if \noinx is not used)
\def\0{\ifx\hide\undefined\egroup\bgroup\let\vb=\\\def\hide{}\bgroup\else\egroup\egroup\bgroup}
